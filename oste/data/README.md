# OSTE - Data
This folder contains supporting files for the translation extraction process.

The `composer.json` and `composer.lock` files in these folders run an intentionally old version
of Drupal and Drush which are needed to run the [POTX](https://drupal.org/project/potx) tool.
