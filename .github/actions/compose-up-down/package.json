{
  "name": "compose-up-down",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "license": "UNLICENSED",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && npm run build:main && npm run build:post",
    "build:main": "ncc build src/main.js -o tmp && mv tmp/index.js dist/main.js && rmdir tmp",
    "build:post": "ncc build src/post.js -o tmp && mv tmp/index.js dist/post.js && rmdir tmp"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
